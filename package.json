{
  "scripts": {
    "dev": "MODE=parseOnly node src/cli.js",
    "staging": "MODE=staging node --env-file=.env src/cli.js",
    "production": "MODE=production node --env-file=.env src/cli.js",
    "deliver:record": "node src/deliverAndRecord.js",
    "test": "node --test"
  },
  "type": "module",
  "dependencies": {
    "@types/node": "^18.11.10",
    "@vercel/node": "^2.7.1",
    "cheerio": "^1.0.0-rc.2",
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}
